pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
pyyaml>=6.0,<7.0
polars>=1.0,<2.0
pandas>=2.0,<4.0  # legacy scripts (backtest_loop.py, data_fetcher.py, generate_test_data.py) import pandas; was missing from deps (pre-existing gap, fixed in chore/ci-deps-hardening)
matplotlib>=3.0,<4.0
seaborn>=0.13,<1.0
numpy>=1.0,<2.0

# --- runtime / allocation engine (runtime/macro_core.py top-level imports) ---
aiosqlite>=0.19,<1.0
httpx>=0.27,<1.0
fastapi>=0.110,<1.0

# --- test / verification ---
pytest>=8.0,<10.0
